//by default all are public.
class Demo{  //can't use access modifier on classes.,can use on nested classes
    name: string;
    private price :  string; //private can access inside class only.
    protected type :string; //protected can access in class and inheritaed class.
    
}
